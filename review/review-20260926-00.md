# 评审 2026-09-26 · 第三轮

评审范围：`00-preliminaries.md` … `12-macros.md`（十三章）。
本轮不查内部一致性 —— 前两轮已经查过，通读结论确认交叉引用有效、内置计数一致、无残留概念。
本轮只问一件事：**这份规范够不够开始实现。**

流程：逐条讨论，定了的打勾、写入结论并改正文；未定的保持 `- [ ]`，不写进正文。
每条固定四段：`现状` → `候选`（待定时给选项）→ `结论`（已定时给理由）→ `状态`。

**引用约定**：只写「文件 + 段落名」，不写行号。

**结论先行**：语义层（类型检查那一半）够实了，可以落代码；语法层和系统层还没定义，现在写编译器会被卡住。
分四组：J 语法层（阻塞 parser）、K 系统层（阻塞产物与运行）、L 设计张力（不定则后面会打架）、M 非阻塞。
N 组是讨论过程中长出来的：属性族、`assert`、以及它们牵出的反射改动。

**进度**：J 组 7/7 全清（J1 文法三段全落地、闭合），K 组 11/11 全清，
L1/N 组大头已定（见文末进度表）。剩余：L2、M 组 4 条、N5（推迟）、N6（暂停）。
语义层 + 语法层 + 系统层均可落代码。

## J. 语法层

- [ ] **J1 没有形式文法**
      现状：十三章用例子说明语法，全文 `grammar` / `syntax` / `keyword` 零命中，
      没有一处 EBNF 或产生式集合。表达式、语句、声明三个层级都没有闭合的定义。
      为什么阻塞：parser 是编译器的第一个模块，没有文法就没有它的输入规格；
      边角问题也无法判定 —— 只能靠「写的时候再决定」，而那些决定会反过来改语义。
      候选：
        A. 一次性写完整的 EBNF，附在 `00-preliminaries.md` 或新增 `13-grammar.md`；
        B. 分章补 —— 每章末尾加「本节涉及的产生式」，随章节演进；
        C. 不写文法，先写 parser，用测试样例固定行为（规范事后追认）。
      倾向：A。B 会让文法散在十三个文件里无法通读，C 等于把规格交给实现。
      要决定的问题：文法单列一章，还是并入 `00`？
      结论（2026-09-26）：A，新章 `15-grammar.md`（13/14 已被 testing/macros 占用；
      章节号只是文件名，README 已明说依赖不是链）。**三段推进，每段消化对应的 J 条**：
      段一词法（J6+J7）→ 段二表达式（J2+J3+J4）→ 段三声明与语句（J5）。
      记法：Wirth EBNF（`=` 定义、`|` 选择、`[...]` 可选、`{...}` 重复、`x … y` 范围）。
      已落地（2026-09-26，段一）：`15-grammar.md` —— Notation / Source text /
      Identifiers and keywords / Integer / Float / Byte and string literals /
      Punctuation / Open items。
      已落地（2026-09-26，段二）：Expressions 节（优先级表 + 全产生式：if/match/block、
      unary/postfix/二元链、path 与 generic_args 含 splice、array/struct literal、
      closure、builtin_call）+ Types 节（`E?T` 一步解析、`[` 前缀、fn/dyn/tuple type）
      + The `?` three ways + The brackets；开放项补移位运算符（v0 无 `<<`/`>>`/`~`，
      `>>` 永远两个 `>`）。写文法时顺手抓到三个正文没定的点：path 中段泛型
      （`is_same<i32, i32>::value`）、generic arg 位置的 splice/lift、
      if 条件处 struct literal 需括号。
      已落地（2026-09-26，段三）：Declarations 节（十种 item + attributes 四槽 +
      generic_params/bound/pack + 参数 const/mut/属性 + enum 两种 payload）+
      Statements 节（let/赋值/跳转/for/块内 const；赋值左式按表达式解析、语义层
      验 place；无 label；块内只许 const 一种 item）+ The for shapes（三形态 +
      `in` 回读 pattern 机制）+ Patterns 节（or/wildcard/path+payload/tuple/
      裸 struct/`..` rest；无 guard 无 range 无字面量 pattern，收编 09）。
      段三顺手补了段二漏的裸 struct literal（`let a: X = {}`，`01` 的 "or" 前形态）。
      **J1 全部落定：文法闭合** —— 每个结构有产生式、每个产生式可回溯到章。
      状态：已落地（三段全清）

- [x] **J2 没有运算符优先级与结合性表**
      现状：`07-operators.md` 定义了运算符如何脱糖到 trait 方法，但没说 `a + b * c` 怎么解析。
      相关：`08-reflection.md` Lifting and splicing 段规定 `^^` 是一元前缀记号而非两个 `^`
      （`^` 是 xor），说明词法层面已经被迫做过一次决定 —— 这类决定还缺一整张表。
      候选：
        A. 沿用 C 的优先级表（熟悉、无惊喜）；
        B. 沿用 Rust 的（与现有语法的相似度更高）；
        C. 大幅简化 —— 强制括号，只保留少数几级（Go 式或更激进）。
      要决定的问题：混合算术与比较时是否要求显式括号？`a < b == c` 要不要合法？
      结论（2026-09-26，随 J1 段二落地 `15-grammar.md` Expressions 节）：
        - **主干跟 C/Rust**：乘 > 加 > 位and > 位xor > 位or > 关系 > `&&` > `||`，
          左结合；位运算按 C 拆三级（`&` > `^` > `|`）。
        - **关系同级不可链（Swift 式防呆）**：`a < b < c` / `a < b == c` 是语法错误，
          连续比较几乎总是数学意图的 bug；bool 比较 `(a < b) == (c < d)` 写括号。
          `&&`/`||` 照常可链（短路链是常态形态）。
        - **赋值是语句不是表达式**（Zig 式）：`a = b = c` 不是形式，右结合问题不存在。
        - **`^^`/`$$` prefix-only**：`a ^^ b` 语法错误（`08` 已定，文法收编）。
        - **v0 无移位、无 `~`**：正文零用例；token 表无 `<<`/`>>`/`~`，
          `is_same<i32, i32>>()` 的 `>>` 天然是两个 `>` —— 嵌套泛型零特殊规则。
          引入移位的成本（lexer 拆 `>>`，Rust 的账）记入 `15` 开放项。
        - **泛型调用 vs 比较的 `<`**：identifier 后贪婪尝试 generic_args，
          闭合 `>` 后跟 `(`/`::`/`.` 才是泛型，否则回退比较（`f<T>(x)` vs `a < b`）。
        - **struct literal 位置限制**：if 条件与 match scrutinee 处（block 将开场）
          需括号 —— `if Point{...} == q` 非法，`if (Point{...}) == q` 合法。
        - **裸 block 不是表达式**：block 只出现在 if 分支 / match arm / 函数体。
        - `?` 传播是后缀（最紧一级）。
      已落地（2026-09-26）：`15-grammar.md` 优先级表 + 产生式 + 决定叙述。
      追加（2026-09-26，PR #8）：**移位引入，`>>` 采纳 Rust 拆分方案**（用户确认）——
      `<<` `>>` `~` `<<=` `>>=` 入 token 表；shift 级插在 additive 与 bit and 之间
      （C 位）；`~` 补进一元层（`!` 是 bool 专用，整数按位反原来无记号）；
      **`>>` 拆分只在 generic_args 闭合点**：lexer 恒发单 token，parser 只在该产生式
      处拆（表达式与类型文法共用），泛型 vs 比较的既有 lookahead 不变。
      语义（`07` What is not a trait 补 Shifts 段）：有符号 `>>` 算术移位、无符号
      逻辑移位（无 `>>>`——无符号类型已表达）；移位量 `>=` 位宽：运行期 panic
      （"shift amount out of range"，one mechanism）、编译期常量则编译错（与常量
      索引越界同一笔交易）；无 trait（built-in for integers，与 `%`/位运算同档）。
      否决：`@shl` 函数形态（高频运算撑不住）、只加 `<<` 不对称、泛型改 `f[i32]`
      （倒因为果）。`15` 开放项删移位条；顺手修段二悬空的 `cmp_operator` 引用。
      状态：已落地

- [x] **J3 `?` 一身三用的词法消歧**
      现状：类型后缀 `?T`（`Option<T>`）与 `E?T`（`Result<T, E>`）、可空指针 `?*T`、
      错误传播运算符 `f()?`（`05-traits.md` Result 段：「no `try`, no exception」）。
      同一个字符在类型位置和表达式位置含义不同，且与 `?` 作为三元/可选链的直觉冲突。
      为什么阻塞：这是词法层面的歧义，必须在写 lexer 前定掉。
      候选：
        A. 保持现状，靠位置区分（类型位置 vs 值位置）；
        B. 传播改用别的记号（如 `try f()`、`f()!`）；
        C. 类型后缀改用别的记号。
      要决定的问题：`Result` 的 `E?T` 写法（错误类型在前）在读到 `E?` 之前无法判定是
      `E` 的类型还是错误类型，词法上要不要给解析器一点提示？
      结论（2026-09-26，随 J1 段二落地 `15-grammar.md` Types 节）：**A，位置消歧正式化**：
        - 类型文法 `result_type = prefix_type [ "?" prefix_type ]` —— `E?T` 一步解析，
          E 省略即 `?T`；两侧皆可嵌套（`E??T` = `Result<Option<T>, E>`）。
        - 表达式 `?` 只有后缀传播（`f()?`），表达式头部出现 `?` 是语法错误。
        - 两套文法不相交，**无需 lexer 提示**——位置即决定。
      已落地（2026-09-26）：`15-grammar.md` Types 节 + The `?` three ways 小节。
      状态：已落地

- [x] **J4 `[` 的七种用法**
      现状：数组类型 `[N]T`；切片类型 `[]T` / `[]mut T`；闭包捕获列表 `fn[x]` /
      `fn[mut n]` / `fn[&mut total]`；索引 `a[i]`；切片区间 `a[1..]`；
      数组字面量 `[3]u32{1, 2, 3}`；属性 `#[...]`（N1 定案后新增）。
      类型位置和表达式位置的区分多半够用，但 `fn[]` 与 `[]T` 相邻出现时需要确认。
      属性那一笔是 `#` 打头且只出现在 item 与参数列表开头，不进表达式解析路径 ——
      对 J2 的优先级表与 J3 的消歧没有影响。
      要决定的问题：捕获列表是否值得独占一个记号（如 `fn|x|`）来换取可读性？
      结论（2026-09-26，随 J1 段二落地 `15-grammar.md`）：
        - 七用各自有产生式：类型前缀（`[N]T` / `[N]mut T` / `[]T` / `[]mut T`）、
          表达式开头的数组字面量（表达式位置唯一合法的 `[` 开头）、
          postfix 索引与区间（`a[i]` / `a[1..2]` / `a[..]`）、`fn` 后捕获列表、
          `#[` 词法 token。
        - **捕获列表保留 `fn[`**：`fn` 是关键字，`[` 紧跟其后不歧义，
          不换 `fn|x|` —— 现有记号无消歧成本，换记号只添一族 token。
      已落地（2026-09-26）：`15-grammar.md` The brackets 小节。
      状态：已落地

- [x] **J5 `for` 三形态消歧，以及 C 风格三段式是否就此消失**
      现状：`10-iteration.md` 的 For 段：`for cond` / `for let PAT = e` / `for PAT in c`，
      靠 `let` 与 `in` 两个记号区分，并说「`in` 出现在语言别处没有，所以一个记号足够」。
      最新提交把 `while` 并入了 `for`（`for cond` 即原来的 `while`），
      但规范里现在没有任何 C 风格 `for (init; cond; step)` 的位置。
      为什么阻塞：不是实现阻塞，是表达力缺口 —— 需要「初始化 + 条件 + 步进」且步进
      不是简单推进迭代器的循环，目前只能写成 `for cond` 加手动维护变量。
      候选：
        A. 接受 —— 用 `for cond` + `mut` 变量写，或者写一个 `Iterator`；
        B. 加第四种形态 `for init; cond; step`；
        C. 由 `Range` + `for in` 覆盖（需要 `Range` 类型和步进支持）。
      要决定的问题：这是刻意的收窄（Zig 也只有 `while` 和 `for`），还是漏了？
      结论（2026-09-26，随 J1 段三落地 `15-grammar.md` The for shapes）：**A，刻意收窄**：
        - 三形态文法闭合：`let` 开头即 for let；否则按 expression 解析，
          后遇 `in` 回读为 pattern（表达式与 pattern 同形，回读零成本）。
        - **C 三段式不引入**：`for cond` + 前置 `let mut` + 体尾步进表达力等价，
          步进即推进时 Iterator 更好；Zig 同款取舍（`while` + `for`，无第三头）。
        - **range 缺口记为开放项**：`..` 只在索引括号内；`for i in 0..10` 需要
          `Range` 迭代器 + range 表达式 —— 需要出现时再加，是加法不是文法缺口。
      已落地（2026-09-26）：`15-grammar.md` for_statement 产生式 + The for shapes 小节。
      状态：已落地

- [x] **J6 字面量与词法基础未定义**
      现状：整数字面量的进制与分隔符、有无类型后缀、浮点形式、`char` 与字符串转义、
      注释形式（`//` / `/* */` / 嵌套）、原始字符串（`01-types.md` Raw String 段有，
      但没说定界规则）、分号与尾随逗号是否可选。
      为什么阻塞：lexer 的第一批规则，不定就没法写 tokenizer。
      要决定的问题：要不要数字分隔符（`1_000_000`）？注释要不要支持嵌套？
      结论（2026-09-26，随 J1 段一落地 `15-grammar.md`）：
        - **注释**：`//` 行注释 + `/* */` 嵌套（必须闭合，不闭合是词法错误）。
        - **分隔符**：`_` 两侧各需一个 digit —— `1_000_000` ✅、`1__0` / `1000_` ❌、
          `_1000` 是 identifier。
        - **进制**：十进制 / `0x` / `0o` / `0b`；无类型后缀（`42u8` 不是形式），
          unsuffixed int → `i32`、float → `f32`（`01` 已有）；负号不是字面量的一部分。
        - **浮点**：仅十进制；`.` 两侧都必须有 digit（`1.` / `.5` 非法）；
          指数 `e[+-]d+`；无小数点有指数（`42e3`）也是 float；无 hex float。
        - **转义集（封闭）**：`\n \t \r \0 \' \" \\ \xNN`（NN 到 `0xFF`，元素是字节，
          字符串是 `[]u8`）；**无 `\u{...}`** —— 源文件是 UTF-8，非 ASCII 字符直接写本体。
        - **字符串前缀**：`c` / `r` / `cr`，前缀必须紧贴 `"`（`c "..."` 是两个 token）；
          raw 无转义、不能含 `"`、无 `r#"..."#` 嵌套（`01` 已定，文法收编）；
          **单行字面量不跨行**。
        - **源文件**：UTF-8，CRLF 归一为 LF，开头 BOM 跳过。
        - 分号/尾随逗号属语法层，段二、段三定。
        - 开放项：Unicode identifier。
      追加（2026-09-26，PR #8）：**多行字符串落地**（用户拍板「要支持下」）——
      Swift 式 `"""`：开引号后必须换行（不进内容）、闭引号独占一行（其前换行不进
      内容）、**闭引号缩进即剥离线**（每内容行剥掉等量前导空白，剥不够是词法错误，
      空行豁免；tab/space 逐字符匹配，混用报错不悄悄错位）；plain 版 8 转义照常、
      raw 版无转义且行内 `"""` 不可表示（plain `\"` 或拼接）；`c`/`r`/`cr` 前缀
      原样组合。备选否决：Zig `\\` 行首（每行噪音）、Python 原样缩进（缩进代码里
      全是垃圾空白）。`01` String 节补 Multiline String 段。
      已落地（2026-09-26）：`15-grammar.md` Source text / Integer / Float /
      Byte and string literals 各节 + Multiline strings 小节；`01-types.md`
      Multiline String 段。
      状态：已落地

- [x] **J7 关键字表缺失**
      现状：`fn` / `struct` / `enum` / `union` / `type` / `const` / `static` / `mut` /
      `impl` / `trait` / `use` / `pub` / `for` / `let` / `if` / `return` / `break` /
      `continue` / `match` / `dyn` 均已出现，但没有一份清单，也没有「保留但未使用」的列表。
      `type` 尤其微妙：`05-traits.md` 里既是关联类型的关键字，也在 `08-reflection.md`
      里成为一种值的类型名（`let t: type = ^^u32`）。
      本轮新增的两条边界：
        - **`extern` 不是关键字** —— K1 定案后它是属性 `#[extern(C)]`，`fn` 的声明语法保持干净。
        - **属性名不是关键字** —— `packed` / `align` / `build` / `test` / `skip`
          都是普通 identifier，由编译器或用户自己解释（N1）。将来加属性不需要扩关键字表。
      结论（2026-09-26，随 J1 段一落地 `15-grammar.md`）：
        - **现役 22 个**：声明 9（`fn struct enum union trait impl type use pub`）/
          值 7（`let const static mut dyn true false`）/ 控制流 7（`if match for in
          break continue return`）。
        - **`true`/`false` 定为关键字**：它们是字面量，`42` 是 lexed not resolved，
          bool 同理；不能被遮蔽。
        - **保留 2 个**：`macro`（A 组定案「若做宏则引入」）/ `defer`（M 组候选名）。
          `async`/`await` 不占 —— 并发连形态都没讨论，赌错方向白占。
        - **非关键字三组写进正文**：`self`（普通参数，`05` 已定）；`Self` / `main` /
          `Some` / `None`（走名字系统）；属性名（identifier）。
        - `type` 的双重身份（关联类型关键字 + `type` 值的类型名）保持现状不拆 ——
          两者都是关键字位置的 `type`，段三文法收编时确认无歧义。
      已落地（2026-09-26）：`15-grammar.md` Identifiers and keywords 节。
      状态：已落地

## K. 系统层

- [x] **K1 没有 ABI / FFI / `unsafe` —— 最要命的一条**
      现状：`01-types.md` Functions 段与 `05-traits.md` Result 段都把 `print` 和 `panic`
      说成「`std` 里的普通函数」，可语言自身没有任何触达系统调用的能力。
      没有 FFI，一个不能 IO 的语言在原理上无法写出 hello world。
      `extern` 只在 `11-namespaces.md` 的开放项和 `README.md` 里出现，未定义。
      候选：
        A. 定义 C ABI 的外部函数声明即可，不引入 `unsafe`；
        B. 引入 `extern` + `unsafe` 块，把「越界/悬垂/别名/FFI」四类交给 `unsafe` 标记；
        C. 不定义 FFI，v0 只做纯计算（能编译、能检查、不能落地）。
      结论：**A，且 `extern` 不作为关键字 —— 改为属性 `#[extern(C)]`。**
        - `#[extern(C)]` + 无函数体 = import；`#[extern(C)]` + 有函数体 = export。
        - `pub` 保留，与 `#[extern(C)]` 是两件事：`pub` 管其他 xyz 代码能不能调，
          `#[extern(C)]` 管 C 能不能调。
        - 为什么 `pub` 不够：xyz 有重载（`04-generics.md` Overloading 段）且目录即命名空间，
          函数符号必须 mangle —— 所以 `pub fn` 导出的符号不叫它在源码里的名字，
          C 那边按字面名找不到。`#[extern(C)]` 标记的是「这个符号按 C 的规则暴露」，
          `pub` 表达不了这件事。
        - 不引入 `unsafe`：xyz 普通代码跨函数边界就已经是 UB（README 的
          Deliberately unchecked 一档），`unsafe` 只能表示「比 UB 更 UB」，语义可疑；
          且 README 已有四档保证，加第五档比加一个关键字便宜。
        - ABI 名字放在属性参数里（`(C)`），不在语法层造 ABI 命名空间。
      已落地（2026-09-26）：`01-types.md` Functions 段新增 `### External functions`
      （import / export、mangling 理由、`pub` 与 `#[extern(C)]` 两问、无 `unsafe`）；
      `00` Notation 与 `12` 的 `#` 构造计数已改为 one —— `#assert` 是唯一的
      非属性 `#` 构造（`#[...]` 一族不在此列）。
      状态：已落地

- [x] **K2 入口点与编译单元 → 产物的构建模型**
      现状：规范里没有 `main`，没有「一个文件 / 一个目录 / 一个库是什么」，
      没有可执行文件与库的区分。`11-namespaces.md` 说「一个目录是一个命名空间」，
      但没说这个目录对应的编译产物是什么。
      为什么它是当前的枢纽：K5（单态化在哪合并）、L1（全程序视图）、
      N9（`#[test]` 的产物与主入口）三条都挂在它下面。
      结论（2026-09-26 全案）：
        - **编译单元 = 整个项目**（根目录树），v0 无分离编译。
          地基是三个既有事实：`04` 的「实例化全局发一份」、`04` 的
          「impl 与 every existing impl 对照」、`11` 的「路径从项目根起算」
          —— 三者都要求编译器全项目视野，分离编译与它们直接冲突。
          代价：改一个文件全量重编（增量化将来靠缓存，不进 v0 规范）；
          库只能源码分发（→ L1 落 a）。
          C / Rust / C++ 的编译单元是它们各自的妥协：C 换编译速度与 .o 分发，
          代价是全局检查不可能；Rust 的 crate 边界让 coherence 只在 crate 内全知
          （著名痛点）；C++ 的模板要求定义可见 → 头文件地狱 + 链接期去重。
          xyz 把妥协反向拿：视野换编译速度。
        - **一项目一产物**：根命名空间有 `fn main` → 可执行；没有 → 库
          （只能被依赖）。同一项目不允许既是又是。不引入 manifest / target。
        - **入口 = 约定名 `fn main`**，在根命名空间、任意文件里。
          不是属性：今天在 `01` 写死了「语言属性全是性质」—— 布局、链接、
          构建模式、控制流、测试；入口是**角色**不是性质，角色用名字表达
          （C / Rust / Zig 全是 `main`）。`#[main]` 会把属性族稀释成「万物皆可标」。
          `main` 不进关键字表（J7 不动）—— 普通名字，编译器按约定找。
          mangle 不管：平台入口符号由编译器的 shim 安排（unmangled 的 C `main`
          调 xyz 的 `main`），规范只说「编译器安排平台入口」。
        - **main 的签名（用户要求：支持 Result）**：`fn main() -> ()` 或
          `fn main() -> E?()`（E 用户定）。返回 `E?()` 时 `?` 在 main 里可用。
        - **退出行为三分**（与 K4d 的 abort 分流）：
          | main 结束方式 | 行为 |
          |---|---|
          | 返回 `Ok` / `()` | `exit(0)` |
          | 返回 `Err(e)` | 打印 e，`exit(1)` —— 正常退出，**不是** abort（Err 是正常错误路径，状态可信） |
          | panic | `abort()`（SIGABRT，K4d） |
        - **Err 的打印**：E 任意，shim 调 `std` 里的反射版打印（`@typeinfo` 看结构、
          enum 打 variant 名、struct 逐字段）—— 相当于免 derive 的 Debug。
          该函数本身值得有（`std::debug` 迟早要打印任意值），实现细节留给 std，
          本轮只定行为。
      已落地（2026-09-26）：`11-namespaces.md` 新增 `## Entry and artifacts` 段。
      追加（2026-09-26，PR #7）：该段连同 Open items 迁至新章 `12-projects.md`，
      `11` 回归纯命名空间。
      状态：已落地

- [x] **K3 没有堆与 allocator**
      现状：`01-types.md` Slice 段明说切片「never allocates」；`10-iteration.md` 的迭代器
      全部栈上。规范里没有出现任何一个 growable 容器，也没有分配/释放的概念。
      为什么阻塞：不是「写不了 `Vec`」，而是「`Vec` 该由谁写、按什么接口写」没有答案 ——
      没有 `unsafe`（K1）就没有写 allocator 的合法位置。
      结论（2026-09-26 全案）：
        - **Allocator 是容器的泛型参数**（用户定案，Zig 式）：`struct Vec<T, A: Allocator>`
          —— `alloc: A` 按值持有（xyz 无引用；多数 allocator 是零大小标记类型，
          真有状态的 arena 就 move 进容器，所有权清楚）。默认走 `04` 的
          Defaults：`Vec<T, A = Heap>`，不写即 `std` 的系统分配器。
        - **a1：`std` 提供系统 allocator**（`std::mem::Heap`）。实现是平台代码，
          内部用 `#[extern(C)]` 调 `malloc` / `mmap` —— 那是 std 的地盘不是用户的，
          K1 的「无 unsafe」与用户侧保证不变。未采纳 a2（`alloc`/`free` 原语进
          builtin —— `@` 家族膨胀、slice never allocates 破功）与 a3（留白 ——
          v0 连 `Vec` 都没有，等于没解）。
        - **trait 签名（用户定案，untyped，无 Layout）**：
          ```rust
          // in std::mem
          enum AllocError { OutOfMemory }

          trait Allocator {
            fn alloc(self: *Self, size: usize, align: usize) -> AllocError?[]u8;
            fn free(self: *Self, buf: []u8) -> ();
          }
          ```
          - 返回 **`AllocError?[]u8`** 不是 `?[]u8`（用户定案）：命名 enum
            将来加变体不破签名；Some/None 表达不了「为什么失败」。
            分配失败的两种实况各走各的糖：返回 null 走 null-capable，
            正式返 Err 走 error forwarding。
          - **不要 `Layout` 封装**（用户定案，推翻本人早先提议）：两个裸
            `usize` 直接传。理由：DTO 会卷进反射体系（`@typeinfo` 描述、
            序列化 walk、`is_same` 撞名），纯搬运参数不值得；Zig 先例同侧
            （`alloc` 直接推参数，不搞 Layout；Rust 用 Layout 是因为
            GlobalAlloc 是跨边界的 unsafe hook）。
          - **untyped，不返回 `[]mut T`**（用户否了 typed 版）：`alloc<T>` 泛型方法
            会让 `dyn Allocator` 没戏（vtable 装不下单态化）。容器侧
            `[]u8` → `[]mut T` 的转换留给 std 的转换函数。
          - **返回 slice 不返回 `?*u8`**（讨论定案）：分配产物天生是 (地址, 长度)
            二元组；slice 有 debug 边界检查而 `*u8` 行走无检查；`01` Pointer 段
            已把分工画好 —— 指针算术是 allocator **内部**走块的工具，
            对**外**的接口给 slice；Rust Allocator 也返回胖指针（`NonNull<[u8]>`）、
            Zig 返回 `[]T`。
          - **`free` 单参数**（讨论定案）：长度已在 `[]u8` 里（类型保证），
            分配器还块靠自己的元数据反查（块头 / size class），不信调用者报的
            Layout —— 传了要么被忽略、要么被错误信任。Rust `deallocate(ptr, layout)`
            成对传是因为它返回裸指针长度不在值里；xyz 的 slice 出厂自带。
            对称性在信息流向不在参数个数：alloc 是调用者→分配器（布局在调用点），
            free 是值本身→分配器（长度在类型里）。
        - **trait 参数不走 `const`**：`dyn Allocator` 的方法必须可动态分发
          （const 方法禁取值，N7）；静态分配器自己优化。
        - **slice never allocates 不破功**：切片仍然不分配 —— 分配的是
          allocator（函数调用），slice 只是分配结果的载体。
        - **命名空间定 `std::mem`**（用户定案，2026-09-26）：`Allocator` /
          `AllocError` / `Heap` 与 `@take` 家族（take / swap / zeroes）同住 mem。
          未采纳 `std::heap`（Allocator 是抽象接口，arena / pool 不是堆，
          名字撒谎；但 `Heap` 与命名空间同名也有自洽性）与 `std::alloc`
          （折中候选）。mem 的辩护：分配器就是「内存来源」，与值的操作
          （take / swap）同属 mem 主题；Rust 的 `std::mem` 放 `take` /
          `swap` / `replace`，Zig 的 `std.mem` 放 `copy` / `zeroes` ——
          两个先例 mem 都不碰分配器，xyz 走自己的路。
      已落地（2026-09-26）：`01-types.md` 新增 `## Allocation` 段。
      状态：已落地

- [x] **K4 `panic` 的落点**
      现状：`01-types.md` Build Modes 段说 debug 下越界/溢出/转换失败会 trap，
      release 下是 UB；`05-traits.md` 说 `panic` 是普通函数。
      但「结束程序」不可能由一个普通函数定义 —— 它必然要落到语言或 ABI 层提供的一个出口。
      结论：**K1 定案后这个问题自己消失了。** 有了 `#[extern(C)]`，`panic` 真的可以是
      普通函数 —— `write(2, ...)` 打印 + `abort()` 终止，不需要任何语言内建。
      已落地（2026-09-26）：`01` 新增 `### Panic` 段；Results 段的 panic 句指向它。
      状态：已落地；子问题 K4a–K4d 均已落地

- [x] **K4a unwind 吗？**
      结论：**不 unwind。** panic = 打印消息 + `abort()`，栈上的 `Drop` 不运行。
        - 析构是 `03-move.md` 静态插入的，只覆盖正常控制流；panic 路径不插。
          正文要明写一句「`Drop` 不保证在 panic 时运行」—— 不能让用户默认它总会跑。
        - 连锁：M3 的 `defer` / `errdefer` 在错误路径上的价值随之削掉一大半
          （panic 直接终止，defer 也没机会跑）—— 已在 M3 记录。
      已落地（2026-09-26）：`01` Panic 段；`03-move.md` Static Insertion 段原有
      「aborts without unwinding」句措辞同步（traps → panics，补 `01` 指向）。
      状态：已落地

- [x] **K4b trap 与 `panic` 是一个机制吗？**
      结论：**统一。** debug 下的越界 / 溢出 / 转换失败不再「trap」一个无名机制，
      而是 panic —— 带具体消息（"index out of range"、"arithmetic overflow" 等，
      文案由实现定），走同一条 abort 路径。一个机制，两个来源：
      语言检查失败（debug 下）与用户调 `assert` / `panic`。
      `trap` 一词从正文消失，`01-types.md` Build Modes 段的 traps 改为 panics；
      debug / release 的差异变成「debug = panic（可诊断），release = UB」。
      已落地（2026-09-26）：`01` Build Modes 段（traps → panics）与 Panic 段
      （one mechanism, not one per cause）。
      状态：已落地

- [x] **K4c 返回类型 / divergence —— 新发现的缺口**
      现状：`10-iteration.md` Divergence 段明确 xyz 没有 `never` 类型，且 divergence
      从**语法**判断（`return` 是语法，看得出来）。但 `panic()` 是函数调用，
      语法上不 divergent，于是：
      `fn f() -> u32 { panic("x"); }` —— body 的值是 `()`，与 `u32` 不匹配。
      结论：**引入 `#[noreturn]` 属性（第五族，控制流）。**
        - 标在函数声明上：调用点按 divergent 处理，与 `return` / `break` / `continue`
          同级 —— `fn f() -> u32 { panic("x"); }` 编译通过（body 全路径 diverge，
          缺失的尾表达式不算类型错误）。
        - `panic` 自己的签名：`#[noreturn] fn panic(msg: []u8)`（返回类型照常省略为
          `()`，值永远不会产生）。用户的 fatal helper 同样可标。
        - 为什么不是另两条：逼用户写 `panic("x"); 0` 是给每个 fatal helper 补哑值；
          `never` 类型与 `10` 冲突且要求类型系统处理子类型合并，复杂度跳升。
          `#[noreturn]` 是局部改动，跟属性族严丝合缝。
        - 代价：`10` Divergence 段的「divergence 从语法判断」放宽为
          「语法 + `#[noreturn]` 标注」。检查是浅的（body 是否存在正常走到末尾的
          路径），不做完整可达性分析 —— 与 `10` 现在的判断同级。
      已落地（2026-09-26）：`01` Attributes 段表 + `10` Divergence 段
      （`#[noreturn]` 调用与 `return` 并列、浅检查说明、`open_or_die` 示例）
      + `01` Panic 段的 `panic` 签名 + `05` Result 段一句。
      状态：已落地

- [x] **K4d 退出码**
      结论：**`abort()`（SIGABRT），不 `exit(1)`。**
        - 与 K4a 配套：状态不可信时不该跑 atexit / flush —— 缓冲区本身可能是坏的。
        - CI 里 SIGABRT（shell 显示 134）是明确的失败信号。
        - 测试产物若想要干净的退出码：运行器用信号处理器转 —— 将来 N9 的事，
          现在不为它改主产物的语义。
      已落地（2026-09-26）：`01` Panic 段（`abort`，not `exit`，含理由）。
      状态：已落地

- [x] **K5 单态化的跨单元合并**
      现状：`04-generics.md` Monomorphization 段：同一实例化全局只发一份，无论多少命名空间调用。
      这与 K2 的编译单元模型直接耦合 —— 单元是什么决定了「全局一份」在哪里合并。
      结论（2026-09-26）：**K2 定案后自行关闭** —— 编译单元是整个项目，
      所有实例化点在单次编译内可见，「全局一份」是编译时去重的平凡结果，
      没有跨单元合并问题。将来若引入分离编译（impl 清单元数据，L1 的 b 方案），
      此条连同「every existing impl」一起重开。
      已落地：无需改正文 —— `04` 的表述在单元 = 全项目下字面成立。
      状态：已关闭（随 K2）

- [x] **K6 编译期求值的执行边界**
      现状：`08-reflection.md` Compile-time execution 段定义了什么能在编译期跑，
      但 `const if` / `const for` / `@typeinfo` 组合起来需要一个能解释 xyz 子集的求值器，
      这是整个编译器里工程量最大的一块。
      结论（2026-09-26）：
        - **判据说死**：输入全部 compile-time known + 求值 no-effects，
          两条判据、无例外无白名单。不设「仅编译期」构造、不按名字禁构造。
          排除表四行（runtime 实参 / alloc / `#[extern(C)]` / panic），
          每行标注被哪条判据挡住 —— 新特性自动归类，不回头维护名单。
          未走 C++ constexpr 的受限小语言路线（十年补丁史的教训）。
        - **四条承诺**（规范的全部约束，不锁实现）：
          1. 确定性（同输入同输出）；
          2. `Drop` 不运行（析构插入运行期控制流，`03`）；
          3. 溢出是编译错误不是 wrap（`01` 编译期检查覆盖求值每一步）；
          4. 求值必须有步数上限，超限编译错误、不得挂死；
             上限数值由实现定（不设默认值，避免成为兼容性锚点）。
        - **budget 小节**：256 展开深度（`04`）与求值步数是两个计数器
          （一个数模板展开、一个数执行步），都超限即错。
        - **不做的**：不造小语言、不限求值器实现方式（解释 / IR / JIT 均可）、
          步数上限不给默认数。
        - 注：与 Zig 划清 —— Zig 的 comptime 是一等机制（关键字、`@Type`
          造类型、类型值混合计算）；xyz 是反射路线，编译期执行是类型检查的
          副产品。K6 只画后者的边界。
      已落地（2026-09-26）：`08` Compile-time execution 段扩写
      （判据 + 排除表 + What the spec promises + Evaluation budget）。
      状态：已落地

- [x] **K7 运行时诊断需要 ABI 层配合**
      现状：`assert` / `panic` 若要报出栈回溯，前提是栈可回溯 —— 保留 frame pointer
      或生成 unwind tables。这是代码生成与 ABI 层面的承诺，与 `#[extern(C)]` 的
      调用约定同级，天然分档：debug 下一定有，release 下 strip 之后可能只剩裸地址。
      结论：**不承诺。** 照 `01-types.md` Build Modes 段的句式写一句：
      backtrace 依赖 frame pointer 或 unwind tables，语言不承诺提供；
      debug 下实现可以保留 frame pointer（成本很低），release 下 strip 之后
      可能只剩裸地址。
      已落地（2026-09-26）：`01` Panic 段最后一段。
      状态：已落地

## L. 设计张力

这两条现在还相安无事，是因为相关的另一方根本没定义；一旦定义就得回头改一个。
改起来现在便宜，拖到实现里贵。

- [ ] **L1 全程序视图 vs 外部库**
      现状：`04-generics.md` 要求重叠在 impl 声明处检查（Overlap Is Checked at Declaration 段），
      并要求同一实例化全局只发一份；两者都需要编译器握有全程序视图。
      而 `12-projects.md` 把「外部库及其路径如何进根」留作开放项。
      一旦库以预编译形态进来，声明处检查就只能在「impl 声明可见」的前提下成立。
      候选：
        A. 库以源码形态分发（保住全程序视图，放弃二进制分发）；
        B. 库带 impl 清单分发（检查跨库可行，但要定义一个元数据格式）；
        C. 重叠检查退化为只在本单元内保证（放弃 `04` 的稳定性承诺）。
      要决定的问题：xyz 要不要支持二进制分发的库？
      结论（2026-09-26）：**落 a —— 库以源码形态分发**（K2 的直接后果：
      编译器要读库源码才能做声明处检查与全局去重）。「依赖如何声明、
      路径如何进根」（H7）保持开放 —— 那是构建系统 / manifest 问题，
      等有形状再谈。将来若要二进制分发，b 的 impl 清单是唯一出路，
      届时 K5 一起重开。
      已落地：`11` Open items 的外部库条目补「source distribution」定性；
      README 的 Still open 同步。
      状态：已落地

- [ ] **L2 「没有 `while`」的表达力代价**
      现状：与 J5 同源，但这是设计层的问题而不是词法问题。
      `for cond` + `mut` 变量、`for in` + `Iterator` 覆盖了绝大多数循环，
      但「条件在本地变量、步进是多语句」的循环会写得比 C 啰嗦。
      要决定的问题：这个代价是设计意图（逼人写 `Iterator`），还是顺带产生的？
      状态：待定

## M. 非阻塞，但迟早要定

- [ ] **M1 并发与原子** —— 全文 `thread` / `atomic` / `volatile` 零命中。
      README 的 What xyz guarantees 已把 data race 列入「Not addressed」，
      但一个 systems language 迟早要表态。
      状态：待定
- [ ] **M2 内联汇编** —— `asm` 零命中。与 K1 的 `unsafe` 是同一组决定
      （K1 定了无 `unsafe`，这条要不要独立存在得更想清楚）。
      状态：待定
- [ ] **M3 `defer` / `errdefer`** —— 清理目前全靠 `Drop`（`03-move.md`）。
      Zig 的经验是有作用域退出钩子会显著减少手写 guard 类型，但也可能只是语法糖。
      K4a 已定「panic 不 unwind、直接 abort」—— defer 在错误路径上的价值随之
      削掉一大半（panic 直接终止，defer 也没机会跑；剩下的是正常路径上的
      早退场景）。讨论这条时要把这个前提算进去。
      状态：待定
- [ ] **M4 `06-dispatch.md` 与 `07-operators.md` 已有的开放项** ——
      `dyn A + B`、`@typeinfo<dyn A>` 的变体、运算符的 `Output` 关联类型、
      `%` 与位运算的 trait。本轮不重复讨论，仅记录它们与本轮 K 组无关。
      状态：待定

## N. 属性族、`assert` 与反射改动

讨论 K1 时长出来的。属性不是宏 —— 不生成代码、不可扩展、不重写语法，
只是编译器认识的固定标记（用户原话：「`#xxx` 是属性 不是宏」）。
字母编号按主题排：N1–N3 属性，N4–N5 断言，N6 宏，N7–N8 反射，N9 测试。

- [x] **N1 属性的语法与结构**
      结论：
        - **写法 `#[...]`** —— 有边界，好解析；无参数的属性也清楚。
          （此前记作 `#xxx`，旧写法已在全节更新为 `#[...]`。）
        - **多个属性两种写法都合法**（Rust 的做法）：
          `#[packed] #[build(debug)]` 与 `#[packed, build(debug)]`。
          注意与 `#[build(debug, release)]` 区分 —— 那是一个属性多个参数。
        - **结构**（`08-reflection.md` 的 `std::meta`）：
          ```rust
          struct Attr { name: []u8, args: []AttrArg }
          enum AttrArg { Ident([]u8), Int(i64), Str([]u8) }   // 不嵌套
          ```
        - **参数是普通 identifier 或字面量**，不是表达式、不是枚举，由属性自己解释
          —— 印证「属性不是函数也不是宏」。
        - **可标位置**：struct / union / enum 声明、enum variant、字段、**函数参数**、
          函数声明。语句与表达式上不允许 —— 属性不进入表达式 / 语句的解析路径，
          利好 J2 的优先级表和 J4 的 `[` 消歧。（参数上允许标，但反射读不到 —— 见 N8。）
        - **两类属性**：编译器消费的（`#[packed]` `#[align]` `#[extern]` `#[build]`
          `#[test]` `#[noreturn]`）与用户反射消费的（`#[skip]` 等，进 `TypeInfo` 的
          `attrs`）。
          前者不需要反射读取 —— 函数**声明**的元信息因此不做，`@fninfo` 不引入。
      已落地（2026-09-26）：`01-types.md` 新增 `## Attributes` 段（语法、两种写法、
      可标位置、参数形态、语言属性表、用户属性走反射）。`#[test]` 未进正文清单
      —— 等 N9 定完再进。
      状态：已落地

- [x] **N2 属性清单：五族 + 用户自定义**
      结论：
        - **布局**：`#[packed]` / `#[align(16)]`。
          **扁平化** —— 不要 `repr`，拆成两个独立属性（原为 `#repr(packed)` /
          `#repr(align(16))`）。互斥规则保留（`02-layout.md` Reprs 段：
          两者同时出现是编译错误），现在是「两个独立属性互斥」而非
          「一个属性的两个参数互斥」。
        - **链接**：`#[extern(C)]`（见 K1）。
        - **构建**：`#[build(debug)]`（见 N3）。
        - **测试**：`#[test]` / `#[test("desc")]`（见 N9）。
        - **控制流**：`#[noreturn]`（见 K4c）—— 调用点按 divergent 处理。
        - **用户自定义**：`#[skip]` 一类，写什么名字由用户定，靠反射读。
      五族正交：布局、链接、构建模式、测试、控制流是五件不相干的事。
      已落地（2026-09-26）：`02` Reprs 段改写为 `## Layout attributes`
      （`#[packed]` / `#[align(N)]`，互斥句保留，含「一个括号或两个都算」）；
      `README` 章节表同步。`#[test]` 待 N9。
      状态：已落地

- [x] **N3 `#[build(debug)]` 的规则**
      结论：release 模式下，该函数的调用点被**整体消除，实参不求值**。
        - 不做声明时的返回值检查，靠使用点报错；但诊断必须明确
          （否则 `let x = debug_only();` 会退化成 "expected expression"）。
        - 参数允许多个值（`#[build(debug, release)]` 等价于无条件）。
        - **禁止 `@take` 出现在 `#[build]` 函数的实参中** —— 否则 release 下
          实参不求值，`03-move.md` 静态插入的析构会在两种模式下对不上。
          覆盖所有 `#[build]` 函数，不只是 `assert`。
        - 不引入 `@mode()`。debug-only 的一段代码靠包函数 + 标 `#[build(debug)]`。
      内置因此维持 10 个 —— `@src()` / `@mode()` / `@text()` 三个候选全部砍掉。
      已落地（2026-09-26）：`01` 新增 `### Mode-gated functions` 段
      （调用点消除、实参不求值、使用点诊断、`@take` 禁令、多模式）。
      状态：已落地

- [x] **N4 `assert` 的定案（运行期）**
      结论：**普通函数 + `#[build(debug)]`**，放在 `std::debug`（该名字由此名副其实）：
      ```rust
      #[build(debug)]
      fn assert(cond: bool) { if !cond { panic("assertion failed"); } }

      #[build(debug)]
      fn assert(cond: bool, msg: []u8) { if !cond { panic(msg); } }
      ```
      三次改形的过程：`#assert`（内置构造）→ `std::assert`（普通函数）→
      `assert!`（宏）→ 回到普通函数。转向宏的理由是「作为函数太薄」：
      拿不到表达式文本、位置依赖构建、release 下关不掉 —— 第三条随后由
      `#[build(debug)]` 解除，于是回到函数。
      接受的限制：表达式文本 ❌ / 位置依赖构建 ⚠️（靠 `panic` 内部的 backtrace，见 K7）。
      **`assert` 只做运行期** —— 编译期那半暂不引入（N5）。
      已落地（2026-09-26）：`01` Mode-gated functions 段（`std::debug` 两个 arity
      的原文进正文）；`12` 补了 `#assert` 与 `assert` 函数的区分。
      追加（2026-09-26，PR #7）：正文全部 `#assert` 改为普通函数调用
      `assert(...)`（`01`/`02`/`03`/`05`/`08`），`00` Notation 补「例子里
      `assert` 裸名指 `std::debug::assert`」。
      状态：已落地

- [ ] **N5 编译期断言：暂不引入**
      现状：正文原有 27 处 `#assert` —— `02-layout.md` 14 处（`@offset` /
      `@sizeof` / `@alignof`）、`08-reflection.md` 10 处（`is_same<...>::value`）、
      `03-move.md` 3 处。
      结论（2026-09-26）：`assert` 只做运行期（N4）；**编译期的暂不引入**。
      追加（2026-09-26，PR #7）：正文 27 处 `#assert` 已全部改为运行期
      `assert(...)` 调用 —— 语法引用一个不引入的构造是错的方向；`00`
      Notation 的 `#` 构造行改为 none、`12` 的 construct 表删除并记录
      deferred。`#` 构造清零，`#[...]` 属性不受影响。
      机制其实已经现成，将来引入不必重趟：`const` 参数 + `const if` +
      `@compileError`（`08-reflection.md` Error Reporting 段原话就是
      「the base for library-defined assertions」）。摸过但未采纳的形态
      （与 N4 的运行期版本同名，靠 arity + const-ness 区分）：
          ```rust
          // in std::debug —— 编译期那一半，暂不引入
          fn assert(const cond: bool) { const if !cond { @compileError("assertion failed"); } }
          fn assert(const cond: bool, const msg: []u8) { const if !cond { @compileError(msg); } }
          ```
      引入那天要连带补两处正文：
        - `04-generics.md` Overloading 段的偏序要纳入 const-ness —— 否则
          `assert(const cond: bool)` 与 `assert(cond: bool)` 在实参编译期已知时
          equally specific，是编译错误（现在 `08` 只说了两者是 distinct overloads）。
        - `08-reflection.md` Compile-time execution 段的 no-effects 规则要
          给 `@compileError` 开一个口子 —— 它不只是「产出值」。
      状态：待定（刻意推迟）

- [ ] **N6 宏：暂停**
      结论：设计成本超出当前需要，停在 B1 定案之后。三条难点已摸清，将来重启不必重趟：
      名字解析需要独立的早期 resolver；卫生性在 token 替换模型下只保护局部变量
      （Rust 的 items 不卫生是已知缺陷）；诊断需要 span 链映射回宏定义。
      A 组七条（若将来做宏）原已定案：引入 `macro` 关键字 / `!` 标记宏名 /
      宏体任意 token / parse 与 expand 交错（Rust 方案）/ 参数原始 token /
      按 arity 重载 / 宏不访问类型信息。
      `14-macros.md`（PR #7 前为 `12`）维持「so far / as it stands」的开放表述。
      状态：待定（刻意暂停）

- [ ] **N7 `TypeInfo` 的改动清单**
      现状：`08-reflection.md` 的 `TypeInfo` 十二个 variant 里没有函数，也没有属性；
      `Struct` / `Union` 带的是 `repr: Repr`。
      结论（已定的部分）：
        - 新增 `Function { params: []Param, ret: type, attrs: []Attr }` ——
          `01-types.md` Functions 段明说函数有类型（`let f: fn(u32) -> u32 = twice;`），
          `@typeinfo<fn(u32) -> u32>()` 目前无处可去。
          闭包不新增 variant：无捕获的闭包即函数类型（`01` Functions 段
          「captures nothing can be used as a plain function pointer」），
          有捕获的是编译器生成的结构体，走 `Struct`。
        - 新增 `Attr` / `AttrArg`（N1）。
        - `Struct` / `Union` 的 `repr: Repr` → `attrs: []Attr`；
          `Field` / `EnumField` 补 `attrs: []Attr`（字段与 variant 都能标属性）。
        - 删 `enum Repr { Default, Packed, Align }`。理由：`@alignof` / `@sizeof` /
          `@offset`（`02-layout.md`）已经能回答全部布局问题，`Repr` 是同一份信息的
          第二个副本，留着要维护两处一致。
        - **编译器生成的 variant 不带 `attrs`**：`Bool` / `Int` / `Float` / `Pointer` /
          `Slice` / `Array` / `Tuple` / `Voidptr` 没有源码位置，attrs 恒空，不为它留字段。
          `TypeInfo` 的字段本来就不统一（`Array` 有 `len`、`Pointer` 有 `child`），
          为一个恒空字段假装统一不划算。
          翻盘条件：将来若允许在**类型别名**上标属性（`type A = #[align(16)] u32;`），
          内建类型的 attrs 就不恒空了。此事与「要不要 type alias」绑定
          （`type` 现在同时是关键字和值类型名，见 J7），现在不开。
      待定：~~`Param` 带不带名字与自己的 attrs~~（2026-09-26 已定，见下）。
        - **`Fn` 最终形态（2026-09-26 定，同日二次修订）**：**一级 variant，不挂在
          `Pointer` 下**：`Fn { args: []FnArg, ret: type }`。自身不带 `attrs`
          （函数类型没有声明位置，`fn(u32) -> u32` 直接写在类型标注里就能用）。
          为什么一级（用户定案）：`*fn(A) -> B` 若函数包在 `Pointer` 里会出现
          两层 `Pointer` 嵌套（函数指针一层 + 真指针一层）；一级化后是干净的
          `Pointer { child: Fn { .. } }`，反射直接 match `Fn { .. }` 也更顺。
          variant 名与 `05` 的 `Fn` trait 撞名但不冲突 —— `TypeInfo` 的 variant
          在 `std::meta` 下，同 `Struct` / `Enum` variant 与关键字同名的先例。
        - **`FnArg` 结构（2026-09-26 定）**：`struct FnArg { name: []u8, type: type }`
          —— 与 `Field` 形状对齐（用户定案）。`name` **恒空**：参数名活在声明里、
          不进类型（`fn twice(x: u32)` 与 `fn double(y: u32)` 是同一类型），
          同 `Tuple` 的 "names are empty" 先例。**不带 `attrs`**：参数属性不进类型
          （N8 落 A），恒空不留字段 —— 与内建 variant 不带 attrs 同一条原则。
        - **`Optional` / `Result` variant（2026-09-26 定，推翻 D2 的反射分流）**：
          `Optional { child: type }`、`Result { ok: type, err: type }`，
          `?T` / `E?T` 一律进自己的 variant，不论有没有 niche。
          动机（用户发现）：`?` 是 `Option<T>` 的语法糖，`optional: true`
          却在讲「这个 Option 走了 niche 布局」—— 纯布局事实进了 `TypeInfo`，
          与删 `Repr` 的理由（布局不解码，靠 `@sizeof`）双标。
          方案（用户定案：学 Zig）：`Pointer` / `Slice` / `Fn` 的 `optional`
          字段全部删除；`?*T` 是 `Optional { child: Pointer{..} }`，
          `?u32` 是 `Optional { child: Int{..} }`；`E?T` 对称走 `Result`。
          Zig 同款结构（`std.builtin.Type`：`optional: Optional` / `error_union`）。
          **D2 前半不动**（`None` / `Some` / `match` / narrowing 对一切 `?T` 一致，
          布局 niche 分流仍是 `02` / `@sizeof` 的事实）；被推翻的是「反射分两路」
          与「`TypeInfo` 不设 `Optional` 变体」两句。`?u32` 塞 `Enum` 会谎报
          tag（`?*T` 无 tag），所以不能走「统一变 `Enum`」那条路。
          泛型代码的收益：识别「可空 / 可错」从两种路径（`Pointer` 带 flag /
          `Enum` 猜 None/Some 形状）变成 match 一个 variant。
      连锁（均已定，2026-09-26）：
        - **`#[noreturn]` 过函数指针即丢**：noreturn 是声明属性不进类型，
          `let f: fn([]u8) -> () = panic;` 合法，但 `f("x")` 不 diverge ——
          编译器照常检查其后的代码（运行期无影响：panic 真的 abort 了）。
          Rust 用 `fn() -> !` 类型保住这条；xyz 接受丢失 —— `#[noreturn]`
          的第二条已知代价（第一条是 `10` 的浅判断放宽）。
        - **带 `const` 参数的函数不能作为值使用（已定）**：赋函数指针、
          作实参、作类型参数都是编译错误。理由：实参烘焙进实例化，
          指针调用没处递 `const` 实参，还会静默绕过编译期已知检查
          （`g(runtime_string)`）。要两用就写两个函数，`const` 版委托普通版。
          Zig 的 comptime 参数同款限制（其 `TypeInfo.Fn.Param.is_generic` +
          `type: ?type` 记录此事）。已落 `08` Const parameters 段。
        - **`#[extern(C)]` 不改调用约定（已定，方案 a）**：xyz 默认约定
          **就是**平台 C 约定 —— 寄存器、栈序，除符号名外全部一致。
          连带：`#[extern(C)]` 函数与普通函数是同一函数类型，可自由作为值
          传递。将来想要更快私有约定的编译器可以整体换 —— 只要它编译
          全程序、约定不越过它自己选的边界。已落 `01` External functions 段
          （首句 "under the C calling convention" 改为 "under the C rules"）。
          未采纳 b（约定进类型，Rust/Zig 的做法）：函数类型分裂，
          xyz 只有两个约定时复杂度不值。
      已落地（2026-09-26）：`08` TypeInfo 段 —— `Struct` / `Union` /
      `Field` / `EnumField` 的 `attrs`、`Attr` / `AttrArg` 定义、`Repr` 枚举删除
      （含「布局不解码，靠 `@sizeof` / `@alignof` / `@offset`」的说明）、
      `Fn` variant 与 `struct FnArg`（一级、不挂 `Pointer` 下的说明与理由）、
      `Optional` / `Result` variant（`optional` 字段全删、分流段重写）；
      `01` Nullability 段反射句同步；`08` Const parameters 段（const 函数
      禁取值 + 两函数模式）；`01` External functions 段（方案 a 三段）。
      状态：已落地（全部闭合）

- [ ] **N8 参数上的属性目前无处可读**
      现状：N1 允许 `fn f(#[unused] x: u32)`，但反射侧拿不到它：
      `TypeInfo::Function` 描述的是**函数类型** `@typeinfo<fn(u32) -> u32>()`，
      参数在这里只是一串类型，没有名字也没有属性；而函数**声明**的元信息（N1）不做，
      `@fninfo` 不引入。即：参数属性既不是编译器消费的（现在没有这样的属性），
      也进不了 `TypeInfo`（没有承载它的声明节点）—— 标了没人读。
      候选：
        A. 参数属性 = 编译器消费，与 `#[extern]` 同类，反射不管。
        B. 为读参数属性引入函数声明反射入口 —— 即复活 `@fninfo`（2026-09-26 已明确不引入）。
        C. 就是死语法，等出现真实消费者再补语义。
      结论（2026-09-26）：**A**。`FnArg` 结构（N7）不带 `attrs`。
      理由比原先硬了一档：不是「选择不读」，是**类型层面根本不存在** ——
      `@typeinfo` 吃的是类型，参数属性活在声明里，要读只能复活 `@fninfo`。
      目前编译器不消费任何参数属性 —— `fn f(#[unused] x: u32)` 是纯语法预留，
      将来编译器要用（如 lint）时才活。
      状态：已定

- [ ] **N9 测试框架：`#[test]` / `#[test("desc")]`**
      结论（已定的部分）：
        - 描述信息是字符串字面量，所以 `AttrArg` 要有 `Str` 变体。
        - 机制：编译器在编译时收集所有标了 `#[test]` 的函数，生成一张表
          ```rust
          struct TestCase {
            name: []u8,          // 函数名
            desc: []u8,          // #[test("...")] 的描述，没写则空
            run:  fn() -> (),
          }
          ```
          运行器遍历执行，失败即 panic。
        - 库做不到 —— xyz 没有遍历所有函数的能力，所以这是编译器特性。
        - `#[test]` 是**编译器消费的属性**，不需要反射读取 —— `@fninfo` 仍不做。
        - **测试是独立产物**，不是 debug 产物里的一个入口。
      待定三条已清（2026-09-26）：
        - **测试函数签名（用户改判：要 Result）**：`fn() -> ()` 与 `fn() -> E?()`
          都合法。`E?()` 版的语义：返回 `Err` 即该测试**失败**，运行器打印
          `Err` 载荷（走 K2 的反射打印）后该测试记失败、继续跑下一个 ——
          **不是** abort、不是跳过。这正是测试里用 `?` 的意义：`let f = open(p)?;`
          开头的测试，Err 直接判死，不用层层 match。验证错误**值**仍用断言
          （`assert(result == Err(expected))`）—— 要验的是值本身时轮不到
          运行器插手。
        - **不建第三种构建模式 `test`**：测试是独立产物，对它而言只有一种
          模式，没有「test 下 vs 非 test 下」的分叉；`#[build]` 照常生效
          （debug-only 代码在测试产物里按 debug 算）。构建模式是
          debug/release 的轴，测试是产物的轴，两轴不相交。
        - **运行器入口**：编译器生成的 shim 自己就是入口（同 main 的 shim
          机制），遍历 `TestCase` 表执行，不需要用户 main；测试产物里
          `fn main` 存不存在都无所谓（收集 `#[test]` 与找 main 是两件独立的
          事）。退出码：全过 `exit(0)`，有挂 `exit(1)`；单测 panic（abort）
          由 shim 转成「test failed」报告 —— 测试死在 panic 里也算失败，
          不算崩溃。`E?()` 版的 Err 失败**不**走 abort（K2 的退出三分是
          main 产物的语义，测试 shim 是另一个入口，Err 是普通控制流）。
      已落地（2026-09-26）：`01` Attributes 段语言属性表补 `#[test]`（第六个）；
      `11` Entry and artifacts 段补测试产物模型。
      追加（2026-09-26，PR #7）：Tests 段迁至新章 `13-testing.md`。
      状态：已落地（全部闭合）

## 进度

| 组 | 已定 / 总数 | 剩下 |
|---|---|---|
| J 语法层 | 7 / 7（全清） | — |
| K 系统层 | 11 / 11（全清） | — |
| L 设计张力 | 1 / 2（L1） | L2 |
| M 非阻塞 | 0 / 4 | 全部，可以一直放着 |
| N 属性与断言 | 7 / 9（N1 N2 N3 N4 N7 N8 N9） | N5 推迟、N6 暂停 |

**K2 已定（2026-09-26）**：编译单元 = 整个项目、约定名 `fn main`（可返回
`E?()`）、退出三分（Ok=exit 0 / Err=打印+exit 1 / panic=abort）、一项目一产物。
K5 随之关闭、L1 落源码分发、N9 解锁。
**K3 已定（2026-09-26）**：allocator 是容器泛型参数、`std::mem::Heap` 默认、
`AllocError?[]u8` + 裸 `(size, align)` 参数（无 Layout）、`free(buf)` 单参。
**N9 已定（2026-09-26）**：测试签名 `()` 与 `E?()` 两型、不建 test 模式、
shim 运行器入口、Err 判败继续跑。
**K6 已定（2026-09-26）**：两条判据说死 + 四条承诺 + 双计数器 budget。
**K 组清零。**
**J6/J7 已定（2026-09-26）**：随 J1 段一（词法文法）落地 `15-grammar.md`：
注释嵌套、`_` 分隔规则、四进制、无后缀、浮点形式、封闭转义集、`c`/`r`/`cr`
前缀、22 关键字 + `macro`/`defer` 保留。
**J2/J3/J4 已定（2026-09-26）**：随 J1 段二（表达式文法）落地 —— 优先级表
（关系不可链、赋值是语句、无移位）、`?` 位置消歧（`E?T` 一步解析）、
`[` 七用各归产生式。
**J5 已定 + J1 闭合（2026-09-26）**：段三（声明与语句）落地 —— 三形态 +
C 三段式不引入 + range 记开放项。**文法闭合，J 组 7/7 全清。**
下一个：L2（「没有 while」的表达力代价）或 M 组 —— 或开始写 parser。
已定条目均已落地正文；例外：N5 的编译期断言（推迟）。
